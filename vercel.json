{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "cleanUrls": true,
  "trailingSlash": false,
  "redirects": [
    { "source": "/sitemap.xml", "destination": "/sitemap-index.xml", "permanent": false }
  ],
  "headers": [
    {
      "comment": "Fonts never change in place — a new weight or subset gets a new filename — so they can be cached hard.",
      "source": "/fonts/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    },
    {
      "comment": "Images change occasionally (client logos, OG cards). Fresh for a week, then served stale while revalidating, so a replaced file lands within days without ever blocking a render.",
      "source": "/images/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=604800, stale-while-revalidate=2592000"
        }
      ]
    }
  ]
}
